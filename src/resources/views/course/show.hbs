<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>Học ngay</button>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <img src="{{course.image}}" alt="Ảnh khóa học">
            <p>{{course.description}}</p>
        </div>
    </div>
</div>